<UserControl x:Class="MVVM.UI.EditButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MVVM.UI"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:ContactViewModel}">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Принять"
                local:DialogResultButton.DialogResult="True"
                Command="{Binding UpdateCommand}" />
        <Button Content="Создать"
                Command="{Binding CreateCommand}" />
        <Button Content="Удалить"
                local:DialogResultButton.DialogResult="True"
                Command="{Binding DeleteCommand}" />
    </StackPanel>
</UserControl>